<?xml version="1.0"?>
<!--
  ClickHouse Node 3 Configuration (Shard 3)
  - Integrated Keeper (server_id=3)
  - Shard cluster member (shard=3)
-->
<clickhouse>
    <!-- Enable listening on all interfaces -->
    <listen_host>0.0.0.0</listen_host>

    <!-- Logging -->
    <logger>
        <level>information</level>
        <console>1</console>
    </logger>

    <!-- ============================================================ -->
    <!-- Macros - Used by ReplicatedMergeTree for path/replica substitution -->
    <!-- ============================================================ -->
    <macros>
        <cluster>shard_cluster</cluster>
        <shard>3</shard>
        <replica>clickhouse3</replica>
    </macros>

    <!-- ============================================================ -->
    <!-- Cluster Definition - 3 shards, 1 replica each (sharding) -->
    <!-- ============================================================ -->
    <remote_servers>
        <shard_cluster>
            <shard>
                <replica>
                    <host>clickhouse1</host>
                    <port>9000</port>
                </replica>
            </shard>
            <shard>
                <replica>
                    <host>clickhouse2</host>
                    <port>9000</port>
                </replica>
            </shard>
            <shard>
                <replica>
                    <host>clickhouse3</host>
                    <port>9000</port>
                </replica>
            </shard>
        </shard_cluster>
    </remote_servers>

    <!-- ============================================================ -->
    <!-- Integrated ClickHouse Keeper Configuration -->
    <!-- ============================================================ -->
    <keeper_server>
        <tcp_port>9181</tcp_port>
        <server_id>3</server_id>
        <log_storage_path>/var/lib/clickhouse/coordination/log</log_storage_path>
        <snapshot_storage_path>/var/lib/clickhouse/coordination/snapshots</snapshot_storage_path>

        <coordination_settings>
            <operation_timeout_ms>10000</operation_timeout_ms>
            <session_timeout_ms>30000</session_timeout_ms>
            <raft_logs_level>warning</raft_logs_level>
        </coordination_settings>

        <raft_configuration>
            <server>
                <id>1</id>
                <hostname>clickhouse1</hostname>
                <port>9234</port>
            </server>
            <server>
                <id>2</id>
                <hostname>clickhouse2</hostname>
                <port>9234</port>
            </server>
            <server>
                <id>3</id>
                <hostname>clickhouse3</hostname>
                <port>9234</port>
            </server>
        </raft_configuration>
    </keeper_server>

    <!-- ============================================================ -->
    <!-- ZooKeeper/Keeper Client Configuration -->
    <!-- Points to the integrated Keeper cluster -->
    <!-- ============================================================ -->
    <zookeeper>
        <node>
            <host>clickhouse1</host>
            <port>9181</port>
        </node>
        <node>
            <host>clickhouse2</host>
            <port>9181</port>
        </node>
        <node>
            <host>clickhouse3</host>
            <port>9181</port>
        </node>
    </zookeeper>

    <!-- Allow DDL queries on cluster -->
    <distributed_ddl>
        <path>/clickhouse/task_queue/ddl</path>
    </distributed_ddl>
</clickhouse>
